/** Divisor for interval bar width: `interval.duration / DIVISOR` */
export const INTERVAL_BAR_WIDTH_DIVISOR = 2
/** Minimum width in px for an interval bar */
export const INTERVAL_BAR_MIN_WIDTH_PX = 24

